{
  "version": 2,
  "builds": [
    {
      "src": "api/*.js", // Adjust the path to your API files
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "api/$1"
    }
  ]
}
